# Prompt boundary

Prompts are provider-neutral cognition inputs, not persistence or authority.
Generic prompt contracts will live here as the context builder and inference
response schemas are implemented. Reference-entity guidance belongs under the
entity configuration, such as `entities/bit/prompts/`.

No prompt may grant a model permission to directly modify identity, traits,
relationships, memories, goals, policies, or embodied actions.
