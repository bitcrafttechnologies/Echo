# Optional direct browser endpoints. Defaults use the local Vite development proxy.
PUBLIC_ECHO_API_URL=
PUBLIC_ECHO_WS_URL=

# Backend target used by the local Vite development proxy.
ECHO_API_PROXY_TARGET=http://127.0.0.1:8000
